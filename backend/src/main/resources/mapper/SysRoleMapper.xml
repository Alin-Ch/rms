<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.www.mapper.SysRoleMapper">

    <select id="getByUserId" resultType="com.www.pojo.SysRole">
        SELECT role.*
        FROM sys_user_role AS user_role
                 JOIN sys_role AS role ON user_role.role_id = role.id
        WHERE user_role.user_id = #{id}
    </select>

</mapper>
