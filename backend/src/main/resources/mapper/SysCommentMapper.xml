<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.www.mapper.SysCommentMapper">

    <select id="getByHouseId" resultType="com.www.pojo.SysComment">
        SELECT c.*, u.username, u.avatar FROM sys_comment c
        LEFT JOIN sys_user u ON c.user_id = u.id
        WHERE parent_id IS NULL
        <if test="#{houseId} != null">
            AND house_id = #{houseId}
        </if>
        ORDER BY create_time DESC
    </select>
    <select id="getReplayList" resultType="com.www.pojo.SysComment">
        SELECT c.*, u.username, u.avatar
        FROM sys_comment c
                 LEFT JOIN sys_user u ON c.user_id = u.id
        WHERE parent_id = #{parentId}
        ORDER BY create_time DESC
    </select>
</mapper>
